{
  "description": "Keeps unfiltered command payloads out of the session. Rewrites in place where the fix is mechanical (pytest gains -q --tb=short, no retry); denies with the fix in the message where narrowing needs a decision the hook cannot make (az without --query, Databricks get/list without a filter, raw REST without jq). Append #full to any command to bypass.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/filter_bash_output.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
