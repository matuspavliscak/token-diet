{
  "description": "Refuses shell commands that would dump an unfiltered payload into the session: az without a query, Databricks get/list without a filter, raw ADO/Databricks REST, and pytest's default report. Append #full to any command to bypass.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/filter_bash_output.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
